function fillFormLanding(fromTown, toTown) {
        document.getElementById("send_from").value = fromTown;
        document.getElementById("send_to").value = toTown;
        location.href = "#";
      }